import {Box, TextApp} from '@component';

export const PaymentAwaited = () => {
  return (
    <Box flex={1} justify="center" align="center">
      <TextApp>PaymentAwaited</TextApp>
    </Box>
  );
};
